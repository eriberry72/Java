public class HomeworkFour {

	public static void main(String[] args) {
		// Find slope when given x1, y1 and x2, y2
		int x1=1, y1=2, x2=3, y2=4;
                int slope;
					
		slope = (y2-y1)/(x2-x1);

		System.out.print("When given two points, (" + x1 + "," + y1 + ") and (" + x2 + "," + y2 + "), the slope is " + slope);
		
	}
}
